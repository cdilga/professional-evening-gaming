# Node Fastify Service Starter

Use `projects/peg-live-lobby/` as the reference implementation.

- Fastify API or websocket service
- independent Node runtime and Docker image
- same private-host deploy pipeline as the other service projects
