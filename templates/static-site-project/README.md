# Static Site Project Starter

Use `projects/peg-nightly-landing/` as the reference implementation.

- standalone static files or generated assets
- Docker image served by nginx
- cloudflared sidecar for private-host exposure
- no database required
