services:
  api:
    depends_on: []
    environment:
      DATABASE_URL: ${DATABASE_URL}
    networks:
      - peg-astacus
      - peg-services

  cloudflared:
    networks:
      - peg-astacus

networks:
  peg-services:
    external: true
    name: peg-services
