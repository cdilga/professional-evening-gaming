# PEG Live Lobby

Realtime service example in a different runtime.

- Node.js + Fastify
- websocket endpoint for live room state
- no forced database dependency
- deploys through the same private-host rollout pattern as the Python services
